PREFIX ldp: <http://www.w3.org/ns/ldp#>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT DISTINCT ?url ?id {

VALUES(?inputUrl) {(<#inputUrl#>)}  

?inputUrl a ldp:Container;
          ldp:contains ?url. 
BIND(REPLACE(str(?url), '^.*(#|/)', "") AS ?id)         
}